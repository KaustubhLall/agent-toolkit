{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"require(require('path').join(process.env.PLUGIN_ROOT,'scripts','lifecycle.cjs'))\"",
            "timeout": 3,
            "additionalContextLimit": 2500
          }
        ]
      }
    ]
  }
}
