# Local vault navigation

Private vault indexes are not distributed in this public bundle. Inspect the
configured vault's Home.md and relevant Index.md files before editing. Verify
the live structure, incoming links and canonical notes instead of assuming a
particular person's folders, projects or history exist.
